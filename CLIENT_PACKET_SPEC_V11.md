# 클라이언트 구현용 통신 및 패킷 명세 — Protocol V11

이 문서는 현재 서버 소스의 실제 동작을 기준으로 작성한 클라이언트 구현 명세다.

- 현재 프로토콜 버전: `11`
- 전송 방식: WebSocket binary frame
- 서버 포트: `9000`
- 서버 simulation tick: `50Hz` (`20ms`, `0.02초`)
- 상태 snapshot 전송 주기: `50Hz` (`20ms`, `0.02초`)
- 멀티세션 방식: WebSocket 연결 하나마다 서로 완전히 독립된 게임 하나

서버는 연결 직후 Type 101 WELCOME으로 프로토콜 버전 `11`을 전송한다. 버전이 다른 클라이언트는 명령 패킷을 보내면 안 된다.

V11 핵심 변경:

- Type 100 Soldier record 끝에 8방향 `direction`을 추가해 record 크기가 28바이트에서 32바이트로 변경됐다.
- Type 8 `MOVE_ENGAGE_ON_SIGHT`와 Type 9 `MOVE_FIRE_IN_RANGE` 이동 전투 명령을 추가했다.
- 게임 simulation과 Type 100 snapshot 전송을 모두 0.02초 간격(50Hz)으로 통일했다.
- Type 101 WELCOME, Type 104 COMMAND_RESULT, Type 105 STAGE_STATE 송신이 활성화됐다.
- Type 102 ATTACK_EVENT와 Type 103 UNIT_DIED는 웹 클라이언트의 이벤트 처리량을 줄이기 위해 예약 번호만 유지하고 송신하지 않는다.
- Type 104로 모든 식별 가능한 클라이언트 명령의 성공·실패 결과를 전송한다.
- Type 105는 스테이지 상태 또는 양 팀 생존 인원이 바뀔 때 전송한다.

V11 서버 최적화 반영 사항(패킷 호환성 유지):

- `PROTOCOL_VERSION`, 활성 패킷의 type, `pkt_len`, 필드 offset, enum 값, 50Hz 전송 주기는 변경되지 않았다.
- 따라서 이미 V11을 구현한 클라이언트는 패킷 코드를 변경할 필요가 없다.
- 서버 내부의 공간 인덱스, 정지 장애물 Grid, JPS 탐색 메모리, 충돌 해결 버퍼, 세션 조회 및 WebSocket 송신 버퍼를 재사용하도록 최적화했다.
- 여러 정지 스쿼드가 동일 목적지를 이미 둘러싼 특수 혼잡 상황에서만 마지막 도착 스쿼드의 내부 응집 허용 반경을 45로 사용한다. 일반 도착 응집 반경 36과 단일 점유 스쿼드 외곽 정지 규칙은 그대로다.

---

## 1. WebSocket 연결

### 1.1 접속 주소

로컬 서버에 직접 접속할 때는 다음 형식을 사용한다.

`ws://<server-address>:9000/`

HTTPS 페이지에서 Cloudflare Tunnel을 통해 접속할 때는 브라우저의 mixed-content 제한 때문에 반드시 다음 형식을 사용한다.

`wss://<cloudflare-tunnel-domain>/`

현재 서버는 WebSocket path, Origin, 애플리케이션 키 또는 로그인 토큰을 검사하지 않는다. WebSocket 표준 핸드셰이크의 `Sec-WebSocket-Key`만 사용한다.

### 1.2 연결 수명주기

1. WebSocket 핸드셰이크가 완료되면 서버가 해당 연결 전용 빈 `GameInstance`를 생성한다.
2. 약 20ms 주기로 Type 100 snapshot을 보내며, 아직 병사가 없으면 `soldierCount = 0`이다.
3. 클라이언트가 명령 패킷을 보내면 그 연결이 소유한 게임에만 적용된다.
4. 연결이 끊기면 해당 게임의 모든 스쿼드와 병사가 삭제된다.
5. 재접속은 새 게임으로 취급되며 이전 `squadID`, `soldierID`, 전투 상태는 복원되지 않는다.

Type 101 WELCOME 수신과 프로토콜 버전 확인을 애플리케이션 통신 준비 완료 기준으로 사용한다.

### 1.3 WebSocket frame 규칙

- 게임 패킷 하나를 WebSocket binary frame 하나에 담는다.
- 한 frame 안에 여러 게임 패킷을 연결해서 보내면 안 된다.
- 하나의 게임 패킷을 여러 WebSocket fragment로 나누면 안 된다.
- text frame은 게임 명령으로 처리되지 않는다.
- 브라우저가 보내는 frame masking은 WebSocket 표준 동작에 맡긴다.
- ping을 보내면 서버가 pong으로 응답한다.
- 현재 명령 패킷의 최대 크기는 32바이트이므로 서버 수신 버퍼 제한에 걸리지 않는다.

---

## 2. 정수 인코딩과 공통 헤더

모든 게임 패킷 필드는 크기 4바이트의 little-endian 정수다. 구조체 padding이나 문자열 구분자는 사용하지 않는다.

| 용도 | 해석 |
|---|---|
| `pkt_type`, `pkt_len`, 인원수, enum, HP, state | signed `int32` |
| `squadID`, `soldierID`, 목적지 좌표 | unsigned `uint32`로 읽어도 됨. 현재 유효 값은 모두 `int32` 양수 범위 안에 있음 |

모든 패킷은 다음 8바이트 헤더로 시작한다.

| Offset | Size | Type | Field | 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 패킷 종류 |
| 4 | 4 | `int32` | `pkt_len` | 헤더를 포함한 게임 패킷 전체 바이트 수 |

`pkt_len`은 WebSocket frame의 binary payload 길이와 정확히 같아야 한다. 값이 다르거나 헤더보다 짧으면 서버는 패킷을 무시한다.

헤더까지 정상적으로 읽을 수 있는 명령은 성공 또는 실패 여부를 Type 104 COMMAND_RESULT로 응답한다. 8바이트 헤더보다 짧은 frame처럼 요청 타입조차 식별할 수 없는 입력만 응답 없이 무시한다.

---

## 3. ID와 소유권 규칙

### 3.1 Session

서버 내부에는 연결마다 `sessionID`가 있으며 Type 101 WELCOME으로 전달된다. 다만 명령 패킷에는 sessionID를 넣지 않으며, 서버는 패킷을 수신한 WebSocket 연결을 기준으로 게임을 선택한다.

서로 다른 WebSocket 연결은 다음 항목을 공유하지 않는다.

- 스쿼드와 병사
- ID 발급 순서
- 좌표와 충돌
- 전투와 승패 상태

따라서 서로 다른 두 클라이언트가 모두 `squadID = 0`을 가질 수 있지만 서로 보거나 공격할 수 없다.

### 3.2 `squadID`

- 한 WebSocket 게임 안에서만 유효하다.
- 아군과 적군이 각각 독립된 번호 공간을 사용한다.
- 각 팀의 첫 생성 스쿼드는 `0`이며, 그 팀에서 생성에 성공할 때마다 `1`씩 증가한다.
- 따라서 아군 0번과 적군 0번이 동시에 존재할 수 있다. 모든 명령과 클라이언트 저장 key는 반드시 `(teamFlag, squadID)`를 사용한다.
- 현재 스쿼드 삭제 패킷은 없으므로 생성된 ID는 연결이 종료될 때까지 유지된다.
- CREATE 성공 시 Type 104의 `teamFlag`, `entityID`로 생성된 스쿼드 ID를 확인한다.

### 3.3 `soldierID`

- 전역 ID가 아니라 해당 스쿼드 내부 ID다.
- 병사를 식별할 때 반드시 `(teamFlag, squadID, soldierID)` 조합을 사용한다.
- 최초 생성 시 해당 스쿼드 안에서 `0`부터 증가한다.
- 최초 생성 순서는 궁수, 전사, 기사 순서다.
- 병사가 다른 스쿼드로 편입되면 대상 스쿼드의 새 `soldierID`를 받는다.
- 편입으로 비어 있는 기존 ID는 다시 채우거나 재정렬하지 않는다.
- 한 번 발급한 ID는 같은 스쿼드 안에서 재사용하지 않는다.

현재 Type 100에는 영구적인 `unitID`와 `unitType`이 없다. 따라서 편입 전후의 동일 병사를 완전히 안전하게 연결할 방법이 없다. 편입을 한 번에 하나씩 보내고, 편입 전후 snapshot의 원본/대상 스쿼드 목록 차이로 새 ID를 확인하는 방식을 권장한다.

---

## 4. 공통 값

### 4.1 맵과 물리 좌표

| 항목 | 값 | 의미 |
|---|---:|---|
| 맵 X 범위 | `0..6399` | MOVE 목적지로 요청 가능한 X |
| 맵 Y 범위 | `0..3199` | MOVE 목적지로 요청 가능한 Y |
| 병사 충돌 반경 | `4` | 병사 중심 기준 원형 반경 |
| 병사 최소 중심 거리 | `8` | 정상 충돌 처리 후 목표 거리 |
| 기본 이동속도 | `20` | 초당 월드 좌표 이동량 |
| 기본 진형 슬롯 간격 | `14` | 생성 및 내부 편입 재정렬에 사용하는 간격 |

병사의 실제 중심 좌표는 충돌 반경 때문에 일반적으로 `X=4..6396`, `Y=4..3196` 안에서 유지된다. MOVE 목적지는 맵 끝 좌표도 받을 수 있지만 실제 병사 좌표는 안전 범위 안으로 보정될 수 있다.

### 4.2 `TeamFlag`

| 값 | 이름 | 의미 |
|---:|---|---|
| 0 | `ALLY` | 아군 |
| 1 | `ENEMY` | 적군 |

팀 값이 같은 병사는 서로 공격하지 않는다. 팀은 스쿼드 단위다. 현재 Type 3 편입은 같은 `teamFlag` 안의 스쿼드 사이에서만 가능하므로 편입으로 팀이 바뀌지는 않는다.

### 4.3 `UnitType`

`UnitType`은 CREATE의 병종별 인원수를 구성할 때 사용한다. 현재 Type 100에는 이 값이 포함되지 않는다.

| 값 | 이름 | 최초 HP | 공격력 | 공격 간격 | 사거리 | 이동속도 |
|---:|---|---:|---:|---:|---:|---:|
| 0 | `ARCHER` / 궁수 | 100 | 10 | 1.0초 | 150 | 20 |
| 1 | `WARRIOR` / 전사 | 150 | 13 | 약 0.667초 | 15 | 20 |
| 2 | `KNIGHT` / 기사 | 200 | 15 | 0.5초 | 20 | 20 |

모든 병종의 일반 적 탐색 시야는 300이다. 공격 간격은 `10 / attackSpeed`로 계산하며, 위 표는 그 결과값이다.

### 4.4 `SoldierState`

Type 100의 `state`는 클라이언트 애니메이션에 사용하는 현재 행동 결과다. 클라이언트가 마지막으로 보낸 장기 명령 자체는 아니다.

| 값 | 이름 | 의미 | 권장 표현 |
|---:|---|---|---|
| 0 | `IDLE` | 현재 이동·추적·공격 동작이 없음 | 대기 |
| 1 | `MOVING` | 일반 이동 또는 유지 위치 복귀 중 | 달리기/걷기 |
| 2 | `CHASING` | 공격 대상을 사거리까지 추적 중 | 전투 이동 |
| 3 | `ATTACKING` | 대상이 사거리 안에 있어 공격 중 | 병종별 공격 모션 |
| 4 | `DEAD` | HP가 0인 최종 상태 | 사망 |
| 5 | `HIT` | 피격 후 약 0.12초 경직 | 피격 모션 |
| 6 | `FORMING` | 정지 상태 편입 후 내부 슬롯 재정렬 중 | 대형 정렬 이동 |

주의 사항:

- 일반 ATTACK 명령을 유지 중이어도 시야 내 적이 없으면 `IDLE`로 보인다.
- STOP 상태 병사가 피격되어 밀린 뒤 유지 위치로 돌아갈 때 잠시 `MOVING`으로 보일 수 있다.
- `ATTACKING`은 실제 피해가 적용되는 순간만 뜻하지 않고, 사거리 안에서 공격 행동을 유지하는 동안 계속 사용된다.
- `HIT`와 `DEAD`는 이전 이동·공격 상태보다 우선한다.
- 현재 snapshot에는 내부 `action` 값이 없으므로 클라이언트가 스쿼드별 마지막 명령을 별도로 기억해야 한다.

### 4.5 `Direction`

Type 100의 `direction`은 병사의 마지막 실제 이동 방향 또는 현재 공격 대상 방향이다. 정지 중에는 마지막 방향을 유지한다.

| 값 | 방향 벡터 | 의미 |
|---:|---|---|
| 0 | `(+X, 0)` | 오른쪽 |
| 1 | `(+X, +Y)` | 오른쪽 아래/위 대각선(클라이언트 Y축 표현에 따름) |
| 2 | `(0, +Y)` | +Y |
| 3 | `(-X, +Y)` | 왼쪽 +Y 대각선 |
| 4 | `(-X, 0)` | 왼쪽 |
| 5 | `(-X, -Y)` | 왼쪽 -Y 대각선 |
| 6 | `(0, -Y)` | -Y |
| 7 | `(+X, -Y)` | 오른쪽 -Y 대각선 |

클라이언트가 화면 좌표에서 Y축을 반대로 표시한다면 위·아래 표현만 뒤집고 숫자 값은 그대로 사용한다. 충돌 보정으로 실제 이동 방향이 바뀌면 서버는 보정이 끝난 방향을 전송한다. `ATTACKING`에서는 이동하지 않더라도 공격 대상을 바라보도록 갱신된다.

---

## 5. 패킷 목록

### Client → Server

| Type | 이름 | 전체 길이 | 명령 |
|---:|---|---:|---|
| 0 | `CREATE_SQUAD` | 32 | 팀, 병종별 인원, 스폰 위치를 지정해 스쿼드 생성 |
| 1 | `MOVE_SQUAD` | 24 | 스쿼드와 팀을 지정해 월드 좌표로 이동 |
| 2 | `ATTACK_SQUAD` | 16 | 스쿼드와 팀을 지정해 가장 가까운 적을 자동 공격 |
| 3 | `TRANSFER_SOLDIER` | 24 | 같은 팀의 다른 스쿼드로 병사 편입 |
| 4 | `STOP_SQUAD` | 16 | 진행 중인 행동을 취소하고 현재 위치에서 정지 |
| 5 | `SWAP_SOLDIER_POSITION` | 24 | STOP 스쿼드 안의 두 병사 자리 맞교환 |
| 6 | `FOCUS_ATTACK` | 24 | 팀까지 지정한 적 스쿼드만 추적하고 공격 |
| 7 | `SET_ATTACK_DAMAGE` | 24 | 특정 생존 병사의 공격력을 전달한 값으로 설정 |
| 8 | `MOVE_ENGAGE_ON_SIGHT` | 24 | 목적지로 이동하다 시야 내 적 발견 시 일반 추격 공격으로 전환 |
| 9 | `MOVE_FIRE_IN_RANGE` | 24 | 이동 중 사거리 안 적만 공격하고 쿨타임에는 목적지 이동 계속 |

### Server → Client

| Type | 이름 | 전체 길이 | 기능 |
|---:|---|---:|---|
| 100 | `SOLDIER_POSITIONS` | `12 + soldierCount × 32` | 현재 게임의 모든 병사 좌표·HP·state·direction snapshot |
| 101 | `WELCOME` | 20 | 프로토콜·세션·서버 틱 정보 |
| 102 | 예약, 송신 안 함 | - | Type 100의 HP/state로 통합 |
| 103 | 예약, 송신 안 함 | - | Type 100의 HP/state로 통합 |
| 104 | `COMMAND_RESULT` | 24 | 클라이언트 명령 성공·실패 결과 |
| 105 | `STAGE_STATE` | 20 | 승패 상태와 양 팀 생존 인원 |

서버가 실제로 전송하는 타입은 100, 101, 104, 105다. 102와 103은 번호 충돌 방지를 위해 예약만 유지한다.

클라이언트 수신기는 WebSocket binary frame마다 먼저 공통 헤더의 `pkt_type`을 읽고 활성 네 타입으로 분기해야 한다. Type 100만 도착한다고 가정하면 WELCOME이나 COMMAND_RESULT를 snapshot으로 잘못 해석하게 된다. 102와 103 처리 코드는 필요 없다.

---

## 6. CREATE_SQUAD — Type 0

지정한 팀, 병종 구성, 스폰 앵커를 사용해 새 스쿼드를 만든다. 아래 32바이트 형식 하나만 유효하며, 기본 형식이나 팀만 지정하는 축약 형식은 지원하지 않는다.

### 6.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `0` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `32` |
| 8 | 4 | `int32` | `archerCount` | 생성할 궁수 수, 0 이상 |
| 12 | 4 | `int32` | `warriorCount` | 생성할 전사 수, 0 이상 |
| 16 | 4 | `int32` | `knightCount` | 생성할 기사 수, 0 이상 |
| 20 | 4 | `int32` | `teamFlag` | `0=ALLY`, `1=ENEMY` |
| 24 | 4 | `int32` | `spawnX` | 스쿼드 생성 앵커 X, `4..6395` |
| 28 | 4 | `int32` | `spawnY` | 스쿼드 생성 앵커 Y, `4..3195` |

### 6.2 검증 조건

- `archerCount + warriorCount + knightCount`는 `1..200`이어야 한다.
- 각 병종 수는 음수일 수 없다.
- `teamFlag`는 0 또는 1이어야 한다.
- `spawnX`, `spawnY`는 위 범위 안이어야 한다.
- 전체 길이가 32바이트가 아니면 무시한다. 20바이트 기본 형식과 24바이트 팀 지정 형식은 유효하지 않다.

### 6.3 처리 결과

- `spawnX`, `spawnY`는 첫 병사 한 명의 절대 위치가 아니라 스쿼드 배치의 기준 앵커다.
- 병사는 기본 GRID 슬롯과 간격 14를 이용해 앵커 주변에 배치된다.
- 이미 다른 병사가 차지한 좌표는 서버가 가까운 빈 좌표를 찾을 수 있다.
- 생성된 모든 생존 병사의 초기 state는 `IDLE`이다.
- 병사 생성 및 최초 `soldierID` 부여 순서는 궁수 → 전사 → 기사다.
- 새 스쿼드와 병사는 다음 Type 100 snapshot에서 확인한다.

서버는 생성 결과 패킷을 따로 보내지 않는다. 여러 CREATE를 동시에 보내면 어느 요청이 어느 새 ID인지 연결하기 어렵기 때문에, ID 확인이 필요할 때는 한 번 생성한 뒤 snapshot에서 새 `squadID`를 확인하고 다음 생성을 보내는 방식을 권장한다.

---

## 7. MOVE_SQUAD — Type 1

스쿼드에 새 목적지를 지정한다. 기존 이동 경로가 있으면 새 목적지 기준으로 교체한다.

### 7.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `1` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `24` |
| 8 | 4 | `uint32` | `squadID` | 해당 팀 안에서 이동시킬 스쿼드 ID |
| 12 | 4 | `int32` | `teamFlag` | 대상 팀, `0=ALLY`, `1=ENEMY` |
| 16 | 4 | `uint32` | `destinationX` | 목적지 X, `0..6399` |
| 20 | 4 | `uint32` | `destinationY` | 목적지 Y, `0..3199` |

### 7.2 동작

- 스쿼드는 JPS로 계산한 공용 경로를 따라 이동한다.
- 이동 중에는 정밀 진형을 강제하지 않고 느슨한 군집으로 이동한다.
- 정지 스쿼드는 동적 장애물로 우회하고, 동시에 이동하는 스쿼드는 국소 회피와 충돌 해결을 사용한다.
- 목적지에 정지 스쿼드가 있으면 요청 좌표에 겹치지 않고 해당 스쿼드 외곽의 안전 지점에서 정지할 수 있다.
- 서로 다른 정지 스쿼드가 둘 이상 동일 목적지를 둘러싼 경우, 마지막 스쿼드는 목적지 주변에서 충분히 응집하면 미세 이동을 반복하지 않고 정지한다. 이 혼잡 전용 응집 반경은 45이며 일반 도착에는 적용하지 않는다.
- 도착 후 현재 배치 그대로 `IDLE`이 되며 자동 진형 정렬은 하지 않는다.
- MOVE는 기존 일반/집중 공격 대상을 해제하고 이동 행동으로 전환한다.

클라이언트는 해당 스쿼드의 생존 병사들이 `MOVING`에서 `IDLE`로 바뀌는 것을 이동 완료 표시로 사용할 수 있다. 도착 위치는 충돌과 점유 목적지 처리 때문에 요청 좌표와 정확히 같지 않을 수 있다.

---

## 8. ATTACK_SQUAD — Type 2

스쿼드의 각 병사가 자신의 일반 시야 안에서 가장 가까운 적을 자동으로 선택해 공격하게 한다.

### 8.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `2` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `16` |
| 8 | 4 | `uint32` | `squadID` | 해당 팀 안에서 자동 공격시킬 스쿼드 ID |
| 12 | 4 | `int32` | `teamFlag` | 대상 팀, `0=ALLY`, `1=ENEMY` |

### 8.2 동작

- 진행 중인 스쿼드 MOVE와 내부 재정렬을 취소하고 공격 행동으로 전환한다.
- 기존 집중 공격 대상은 해제된다.
- 각 생존 병사는 반경 300 안의 살아 있는 다른 팀 병사 중 가장 가까운 대상을 개별적으로 선택한다.
- 대상이 사거리 밖이면 `CHASING`, 사거리 안이면 `ATTACKING` 상태가 된다.
- 추적 경로는 최대 0.25초 간격으로 다시 계산한다.
- 공격 대상이 죽거나 시야 밖으로 나가면 새 대상을 찾는다.
- 적이 없으면 state는 `IDLE`이지만 ATTACK 명령은 유지되므로, 이후 적이 시야에 들어오면 자동으로 공격을 시작한다.
- STOP 또는 MOVE 같은 다른 명령을 받을 때까지 자동 공격 행동을 유지한다.

피해량, 공격 간격과 사거리는 4.3의 병종 표를 따른다. 실제 타격 결과는 다음 Type 100의 HP 변화로, 사망은 `HP=0`, `state=DEAD(4)`로 확인한다. 별도 공격·사망 이벤트는 전송하지 않는다.

---

## 9. TRANSFER_SOLDIER — Type 3

현재 스쿼드에 있는 병사 한 명을 같은 게임의 다른 스쿼드로 편입한다.

### 9.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `3` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `24` |
| 8 | 4 | `int32` | `teamFlag` | 원본과 대상 스쿼드의 팀 |
| 12 | 4 | `uint32` | `soldierID` | 현재 스쿼드 안에서의 병사 ID |
| 16 | 4 | `uint32` | `currentSquadID` | 병사가 현재 속한 원본 스쿼드 ID |
| 20 | 4 | `uint32` | `nextSquadID` | 병사를 받을 같은 팀의 대상 스쿼드 ID |

### 9.2 실행 조건

- 원본 스쿼드와 대상 스쿼드가 모두 현재 WebSocket 게임에 존재해야 한다.
- `currentSquadID`와 `nextSquadID`는 서로 달라야 한다.
- `soldierID`가 원본 스쿼드에 실제로 존재해야 한다.
- 현재 서버는 사망 병사를 명시적으로 거부하지 않지만, 클라이언트는 `HP > 0`인 병사만 편입해야 한다.
- CREATE에는 스쿼드당 200명 제한이 있지만 TRANSFER의 대상 인원 상한은 현재 별도로 검사하지 않는다. 클라이언트는 대상 스쿼드도 200명 이하로 유지하는 것을 권장한다.

### 9.3 처리 결과

- 병사의 이전 이동, 공격, 집중 대상, 회피 상태가 초기화된다.
- 병사의 `squadID`는 `nextSquadID`로 바뀐다.
- 원본과 대상은 같은 `teamFlag`로 조회하므로 이 패킷으로 병사의 팀은 바뀌지 않는다.
- 병사의 `soldierID`는 대상 스쿼드에서 새로 발급된다.
- 원본 스쿼드의 빠진 ID는 재정렬하거나 재사용하지 않는다.
- 원본 또는 대상 스쿼드가 공용 MOVE 중이 아니면 새 인원 구성으로 내부 재정렬하며 그동안 `FORMING`이 보일 수 있다. 공격 대기 중인 스쿼드도 이 재정렬로 전환되면서 기존 공격 행동이 취소될 수 있다.
- 대상 스쿼드가 이동 중이었다면 편입 병사가 대상의 공용 이동 경로에 합류한다.

편입 명령 결과는 Type 104로 확인한다. 다만 영구 `unitID`가 없으므로 새 `soldierID`는 명령 전후 Type 100의 원본·대상 목록 차이로 확인해야 한다.

---

## 10. STOP_SQUAD — Type 4

스쿼드의 이동, 일반 공격, 집중 공격과 내부 재정렬을 중단하고 현재 위치를 새 유지 위치로 설정한다.

### 10.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `4` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `16` |
| 8 | 4 | `int32` | `teamFlag` | 대상 팀, `0=ALLY`, `1=ENEMY` |
| 12 | 4 | `uint32` | `squadID` | 해당 팀 안에서 정지시킬 스쿼드 ID |

### 10.2 동작

- 기존 공용 경로와 병사별 경로를 삭제한다.
- 일반 공격과 집중 공격 대상을 해제한다.
- 각 생존 병사의 현재 좌표를 새 유지 위치로 저장한다.
- 정상적인 생존 병사는 다음 update에서 `IDLE`이 된다.
- 명령 직전 피격 경직이 남아 있으면 짧게 `HIT`이 유지된 뒤 `IDLE`이 될 수 있다.
- 사망 병사의 state는 `DEAD`로 유지된다.

Type 5 위치 교환을 실행하려면 먼저 STOP을 보내고, Type 100에서 모든 `HP > 0` 병사가 `IDLE`인지 확인해야 한다.

---

## 11. SWAP_SOLDIER_POSITION — Type 5

STOP 상태인 스쿼드 안에서 지정한 두 생존 병사의 현재 자리를 즉시 맞바꾼다. 진형 전체를 다시 계산하거나 이동시키는 명령이 아니다.

### 11.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `5` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `24` |
| 8 | 4 | `int32` | `teamFlag` | 대상 팀, `0=ALLY`, `1=ENEMY` |
| 12 | 4 | `uint32` | `squadID` | 위치를 바꿀 두 병사가 속한 스쿼드 ID |
| 16 | 4 | `uint32` | `firstSoldierID` | 첫 번째 생존 병사의 현재 스쿼드 내부 ID |
| 20 | 4 | `uint32` | `secondSoldierID` | 두 번째 생존 병사의 현재 스쿼드 내부 ID |

### 11.2 실행 조건

- 두 ID는 서로 달라야 한다.
- 두 병사가 모두 지정한 스쿼드에 존재하고 살아 있어야 한다.
- 스쿼드의 모든 생존 병사가 내부적으로 `STOP` 명령 상태여야 한다.
- 스쿼드의 모든 생존 병사가 Type 100 기준 `IDLE`이어야 한다.
- MOVE 또는 편입 후 `FORMING`이 진행 중이면 실행하지 않는다.

ATTACK 명령 중 적이 없어 snapshot state만 `IDLE`인 경우에도 내부 action은 STOP이 아니므로 교환하지 않는다. 반드시 Type 4 STOP을 먼저 보내야 한다.

### 11.3 처리 결과

- 두 병사의 `posX`, `posY`가 맞바뀐다.
- 다음 이동과 편입 재정렬에서 새 자리를 유지하도록 내부 슬롯 소유권도 함께 바뀐다.
- 기존 두 좌표만 사용하므로 스쿼드가 점유한 전체 좌표 집합은 변하지 않는다.
- JPS 경로 탐색, 이동, 충돌 해결을 실행하지 않는다.
- `FORMING`이나 `MOVING`으로 전환하지 않고 `IDLE`을 유지한다.

Type 104 성공 응답 후 다음 Type 100에서 두 `(teamFlag, squadID, soldierID)`의 좌표가 서로 바뀌었는지 확인한다. 여러 병사의 순서를 바꾸려면 필요한 병사 쌍을 하나씩 교환하면 된다.

---

## 12. FOCUS_ATTACK — Type 6

자기 스쿼드가 지정한 적 스쿼드만 추적하고 공격하게 한다.

### 12.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `6` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `24` |
| 8 | 4 | `int32` | `ownTeamFlag` | 명령을 받을 팀 |
| 12 | 4 | `uint32` | `ownSquadID` | 해당 팀 안의 명령 대상 스쿼드 ID |
| 16 | 4 | `int32` | `targetTeamFlag` | 집중 공격할 상대 팀 |
| 20 | 4 | `uint32` | `targetSquadID` | 상대 팀 안의 대상 스쿼드 ID |

### 12.2 실행 조건

- 두 스쿼드가 모두 같은 WebSocket 게임에 존재해야 한다.
- 두 스쿼드의 `teamFlag`가 달라야 한다.
- 팀별 ID 공간이므로 두 스쿼드의 숫자 ID가 같아도 정상이다. 예: 아군 0번이 적군 0번을 공격할 수 있다.

### 12.3 동작

- 각 생존 병사는 지정한 target 스쿼드 안에서 가장 가까운 살아 있는 병사를 선택한다.
- 다른 적 스쿼드가 더 가까이 있어도 공격하지 않는다.
- 집중 탐색 반경은 6400이다.
- 대상이 사거리 밖이면 `CHASING`, 사거리 안이면 `ATTACKING`이 된다.
- 현재 대상 병사가 죽으면 같은 target 스쿼드의 다음 생존 병사를 찾는다.
- target 스쿼드에 생존 병사가 없으면 state는 `IDLE`이지만 집중 대상 설정은 유지된다.

현재 구현에서 FOCUS_ATTACK은 진행 중인 스쿼드 공용 MOVE 플래그를 직접 취소하지 않는다. 이동 중인 스쿼드를 즉시 집중 공격시키려면 `STOP_SQUAD`를 먼저 보내고 `IDLE` snapshot을 확인한 뒤 FOCUS_ATTACK을 보내야 한다.

일반 ATTACK으로 되돌리려면 Type 2를 보내면 되고, 집중 공격을 완전히 취소하려면 STOP 또는 MOVE를 보내면 된다.

---

## 13. SET_ATTACK_DAMAGE — Type 7

같은 게임 안의 특정 생존 병사 한 명의 공격력을 전달한 값으로 설정한다. 현재 연결이 유지되는 동안 또는 병사가 사망할 때까지 설정값이 유지되며, 스쿼드 편입 후에도 유지된다.

### 13.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `7` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `24` |
| 8 | 4 | `int32` | `teamFlag` | 대상 팀, `0=ALLY`, `1=ENEMY` |
| 12 | 4 | `uint32` | `squadID` | 해당 팀 안에서 대상 병사가 속한 스쿼드 ID |
| 16 | 4 | `uint32` | `soldierID` | 공격력을 설정할 현재 스쿼드 내부 병사 ID |
| 20 | 4 | `int32` | `attackDamage` | 새 공격력, `1..2147483647` |

### 13.2 실행 조건

- `squadID`가 현재 WebSocket 게임에 존재해야 한다.
- `(teamFlag, squadID, soldierID)`에 해당하는 병사가 존재하고 살아 있어야 한다.
- `attackDamage`는 0보다 커야 한다.

### 13.3 동작

- 새 공격력은 기존 값과 관계없이 정확히 `attackDamage`가 된다.
- 공격 중에 명령을 받아도 현재 공격 행동을 취소하지 않으며 다음 실제 타격부터 새 공격력이 적용된다.
- 병사의 HP, 좌표, action, state, 공격속도와 사거리는 변경하지 않는다.
- 같은 병사에게 여러 번 보내도 누적되지 않으며 마지막으로 전달한 값으로 덮어쓴다.
- 병사가 다른 스쿼드로 편입되어도 설정한 공격력은 유지된다. 이후 명령에는 편입 후 새 `(teamFlag, squadID, soldierID)`를 사용해야 한다.

설정 성공 여부는 Type 104로 확인한다. Type 100에는 공격력 필드가 없으므로 UI에 표시할 값은 성공 응답을 받은 마지막 설정값을 클라이언트가 보관해야 한다.

---

## 14. MOVE_ENGAGE_ON_SIGHT — Type 8

목적지로 이동하면서 일반 시야 반경 300 안의 적을 탐색한다. 스쿼드 병사 중 한 명이라도 적을 발견하면 이동 목적지를 포기하고 스쿼드 전체가 기존 Type 2와 같은 일반 추격 공격으로 전환한다.

### 14.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `8` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `24` |
| 8 | 4 | `int32` | `teamFlag` | 대상 팀, `0=ALLY`, `1=ENEMY` |
| 12 | 4 | `uint32` | `squadID` | 해당 팀 안에서 명령을 받을 스쿼드 ID |
| 16 | 4 | `int32` | `destinationX` | 목적지 X, `0..6399` |
| 20 | 4 | `int32` | `destinationY` | 목적지 Y, `0..3199` |

### 14.2 동작

- 적을 발견하기 전에는 Type 1과 동일한 공용 경로·충돌·우회 이동을 사용하며 state는 `MOVING`이다.
- 한 병사가 적을 발견하면 모든 생존 병사가 `AC_ATK`로 전환한다.
- 전환 이후 각 병사는 시야 내 가장 가까운 적을 선택하며, 사거리 밖이면 `CHASING`, 사거리 안이면 `ATTACKING`이 된다.
- 전환 시 원래 목적지와 이동 경로는 취소된다. 전투가 끝난 뒤 목적지 이동을 재개하려면 클라이언트가 새 이동 명령을 보내야 한다.
- STOP, MOVE 또는 다른 명령을 보내면 현재 행동을 교체한다.

---

## 15. MOVE_FIRE_IN_RANGE — Type 9

목적지로 계속 이동하되, 각 병사는 자기 공격 사거리 안에 이미 들어온 적만 공격한다. 적을 추적하거나 원래 경로를 적 위치로 바꾸지 않는다.

### 15.1 레이아웃

| Offset | Size | Type | Field | 허용 값과 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `9` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `24` |
| 8 | 4 | `int32` | `teamFlag` | 대상 팀, `0=ALLY`, `1=ENEMY` |
| 12 | 4 | `uint32` | `squadID` | 해당 팀 안에서 명령을 받을 스쿼드 ID |
| 16 | 4 | `int32` | `destinationX` | 목적지 X, `0..6399` |
| 20 | 4 | `int32` | `destinationY` | 목적지 Y, `0..3199` |

### 15.2 동작

- 공격 준비가 끝났고 적이 사거리 안에 있으면 그 tick에는 정지해 가장 가까운 적을 한 번 공격하며 `ATTACKING`이 된다.
- 공격 쿨타임 중에는 적을 따라가지 않고 원래 스쿼드 목적지를 향해 계속 이동하며 `MOVING`이 된다.
- 궁수·전사·기사별 공격 사거리와 쿨타임은 4.3 표의 값을 사용한다.
- 이 명령에서는 `CHASING` 상태가 발생하지 않는다.
- 적이 시야에는 있지만 공격 사거리 밖에 있다면 무시하고 이동한다.
- 목적지에 도착하면 현재 위치에서 `IDLE`로 끝나며, 이후 자동 공격을 유지하지 않는다.

---

## 16. SOLDIER_POSITIONS — Type 100

해당 WebSocket 연결이 소유한 게임의 모든 병사를 0.02초 간격, 약 50Hz로 전송하는 authoritative snapshot이다.

### 16.1 전체 레이아웃

전체 길이 공식:

`pkt_len = 12 + soldierCount × 32`

| Offset | Size | Type | Field | 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `100` |
| 4 | 4 | `int32` | `pkt_len` | 이 snapshot의 전체 바이트 수 |
| 8 | 4 | `int32` | `soldierCount` | 뒤에 이어지는 Soldier record 수, 0 이상 |
| 12 | `32 × N` | record | `soldiers` | Soldier record가 `soldierCount`회 반복 |

병사가 없는 정상 snapshot은 `pkt_type=100`, `pkt_len=12`, `soldierCount=0`이다.

### 16.2 Soldier record

각 record의 시작 offset은 다음과 같다.

`recordStart = 12 + soldierIndex × 32`

| Record offset | Size | Type | Field | 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `squadID` | 현재 소속 스쿼드 ID |
| 4 | 4 | `int32` | `soldierID` | 현재 스쿼드 내부 병사 ID |
| 8 | 4 | `int32` | `teamFlag` | `0=ALLY`, `1=ENEMY` |
| 12 | 4 | `int32` | `posX` | 현재 월드 X 좌표 |
| 16 | 4 | `int32` | `posY` | 현재 월드 Y 좌표 |
| 20 | 4 | `int32` | `HP` | 현재 체력, 사망 시 0 |
| 24 | 4 | `int32` | `state` | 4.4의 `SoldierState` 값 |
| 28 | 4 | `int32` | `direction` | 4.5의 8방향 값 |

### 16.3 처리 규칙

- snapshot에는 살아 있는 병사와 죽은 병사가 모두 포함된다.
- 죽은 병사는 `HP=0`, `state=DEAD(4)`로 계속 포함된다.
- 병사 배열 순서는 프로토콜로 보장되지 않으므로 배열 index를 병사 ID로 사용하면 안 된다.
- 항상 `(teamFlag, squadID, soldierID)`를 key로 사용한다.
- 편입 시 두 key가 모두 바뀔 수 있으므로 이전 key가 사라지고 대상 스쿼드에 새 key가 생긴다.
- 클라이언트는 수신할 때 `pkt_len == 실제 frame payload 길이`인지 확인해야 한다.
- 클라이언트는 `실제 frame payload 길이 == 12 + soldierCount × 32`인지도 확인해야 한다.
- 서버 좌표가 authoritative하므로 화면 이동은 연속 snapshot 사이를 보간해 표현하는 것을 권장한다.

### 16.4 현재 포함되지 않는 정보

Type 100에는 다음 값이 없다.

- `unitType`
- 내부 장기 `action`
- 영구 `unitID`
- snapshot sequence 또는 server tick
- 현재 공격력 설정값
- command request ID

클라이언트는 최초 CREATE 요청의 병종 수와 초기 ID 순서로 병종을 임시 관리할 수 있다. 하지만 편입 후에는 영구 ID와 `unitType`이 없으므로 완전한 추적이 보장되지 않는다.

---

## 17. 명령별 state 흐름

| 마지막 명령/상황 | 일반적인 state 흐름 |
|---|---|
| CREATE | `IDLE` |
| MOVE | `MOVING → IDLE` |
| ATTACK, 적 없음 | `IDLE`이지만 ATTACK action 유지 |
| ATTACK, 적이 멂 | `CHASING` |
| ATTACK, 적이 사거리 안 | `ATTACKING` |
| 피격 | 기존 상태 → `HIT` → 기존 action에 맞는 상태 |
| 사망 | `DEAD` |
| 정지 상태에서 편입 | `FORMING → IDLE` |
| 이동 상태에서 편입 | 편입 병사와 대상 스쿼드가 `MOVING`, 도착 후 `IDLE` |
| STOP | `IDLE` 또는 남은 피격 경직 동안 잠시 `HIT → IDLE` |
| SWAP_SOLDIER_POSITION | 계속 `IDLE`, 두 병사 좌표만 교환 |
| SET_ATTACK_DAMAGE | 현재 state 유지, 다음 실제 타격부터 설정한 공격력 적용 |
| MOVE_ENGAGE_ON_SIGHT | `MOVING → CHASING/ATTACKING`, 발견 후 원래 목적지 취소 |
| MOVE_FIRE_IN_RANGE | 평소 `MOVING`, 실제 타격 tick에만 `ATTACKING`, `CHASING` 없음 |

state만으로 장기 명령을 역추론하면 안 된다. 예를 들어 `IDLE`은 STOP뿐 아니라 “ATTACK 중이지만 적 없음”도 의미한다.

---

## 18. 클라이언트 상태 관리 권장사항

1. Type 100은 매번 현재 authoritative 상태로 적용한다.
2. 병사 map의 key는 배열 index가 아니라 `(teamFlag, squadID, soldierID)`로 둔다.
3. 스쿼드별로 클라이언트가 마지막으로 보낸 명령을 별도 저장한다.
4. 모든 명령의 Type 104 결과를 확인하고, 좌표·명단 변경은 다음 Type 100으로 확정한다.
5. Type 5는 반드시 STOP을 먼저 보낸 뒤 모든 생존 병사의 `IDLE`을 확인하고 전송한다.
6. 이동 중 FOCUS_ATTACK은 STOP 확인 후 전송한다.
7. 죽은 병사는 snapshot에서 즉시 삭제하지 말고 사망 모션 처리 후 `HP=0`, `DEAD` record를 기준으로 관리한다.
8. 연결이 끊어지면 기존 ID와 게임 상태를 폐기한다. 재접속은 새 게임이다.
9. unknown server packet은 연결을 끊지 말고 안전하게 건너뛸 수 있도록 구현하는 것이 좋다.
10. 공격력 설정 결과는 Type 100에 직접 나오지 않으므로, UI에 표시하려면 클라이언트가 마지막으로 보낸 설정값을 별도로 관리해야 한다.

---

## 19. WELCOME — Type 101

WebSocket 핸드셰이크가 완료되고 독립 게임 인스턴스가 생성되면 한 번 전송한다.

| Offset | Size | Type | Field | 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `101` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `20` |
| 8 | 4 | `int32` | `protocolVersion` | 현재 `11` |
| 12 | 4 | `int32` | `sessionID` | 연결에 할당된 서버 세션 ID |
| 16 | 4 | `int32` | `serverTickMs` | 고정 simulation tick, 현재 `20` |

---

## 20. 예약 패킷 — Type 102

Type 102는 이전 `ATTACK_EVENT`용 번호지만 현재 서버는 송신하지 않는다. 클라이언트는 Type 100에서 이전 snapshot과 현재 snapshot의 HP 차이를 사용해 피해를 확인한다. 공격 모션은 `state=ATTACKING(3)`을 사용한다.

---

## 21. 예약 패킷 — Type 103

Type 103은 이전 `UNIT_DIED`용 번호지만 현재 서버는 송신하지 않는다. 클라이언트는 Type 100의 `HP=0`과 `state=DEAD(4)`를 사망 기준으로 사용한다. 현재 활성 패킷만으로는 처치자 정보가 제공되지 않는다.

---

## 22. COMMAND_RESULT — Type 104

헤더에서 요청 타입을 판별할 수 있는 모든 클라이언트 명령에 한 번 응답한다. 전체 길이는 `24`바이트다.

| Offset | Size | Type | Field | 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `104` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `24` |
| 8 | 4 | `int32` | `requestPacketType` | 처리한 Client→Server 패킷 타입 |
| 12 | 4 | `int32` | `resultCode` | 아래 결과 코드 |
| 16 | 4 | `int32` | `teamFlag` | 주 대상 팀, 판별 불가하면 `-1` |
| 20 | 4 | `int32` | `entityID` | 주 대상 또는 생성된 `squadID`, 판별 불가하면 `-1` |

| resultCode | 이름 | 의미 |
|---:|---|---|
| 0 | `OK` | 명령 적용 완료 |
| -1 | `INVALID_PAYLOAD` | 길이, enum, 좌표 또는 값 범위 오류 |
| -2 | `NOT_OWNER` | 소유권 오류용 예약값. 독립 게임 구조에서는 일반적으로 사용하지 않음 |
| -3 | `NOT_FOUND` | 지정 스쿼드 또는 병사를 찾지 못함 |
| -4 | `INVALID_STATE` | 같은 스쿼드 편입, STOP이 아닌 상태의 교환 등 실행 상태 오류 |

현재 request ID는 없으므로 같은 타입 명령을 동시에 여러 개 보내면 응답을 개별 요청과 완전히 연결할 수 없다.

`entityID`의 구체적인 의미는 요청 타입에 따라 다음과 같다.

| requestPacketType | 성공 시 `teamFlag` | 성공 시 `entityID` |
|---:|---|---|
| 0 CREATE | 생성된 팀 | 새 `squadID` |
| 1 MOVE | 대상 팀 | 대상 `squadID` |
| 2 ATTACK | 대상 팀 | 대상 `squadID` |
| 3 TRANSFER | 대상 팀 | `nextSquadID` |
| 4 STOP | 대상 팀 | 대상 `squadID` |
| 5 SWAP | 대상 팀 | 대상 `squadID` |
| 6 FOCUS_ATTACK | 공격 명령 팀 | `ownSquadID` |
| 7 SET_ATTACK_DAMAGE | 대상 팀 | 대상 `squadID` |
| 8 MOVE_ENGAGE_ON_SIGHT | 대상 팀 | 대상 `squadID` |
| 9 MOVE_FIRE_IN_RANGE | 대상 팀 | 대상 `squadID` |

고정 길이 오류처럼 payload를 해석할 수 없으면 `teamFlag=-1`, `entityID=-1`이다. 존재하지 않는 ID처럼 payload를 해석한 뒤 실패한 경우에는 가능한 범위에서 요청에 들어 있던 팀과 스쿼드 ID를 되돌려준다.

---

## 23. STAGE_STATE — Type 105

스테이지 상태 또는 어느 팀의 생존 인원이 바뀔 때 전송한다. 전체 길이는 `20`바이트다.

| Offset | Size | Type | Field | 의미 |
|---:|---:|---|---|---|
| 0 | 4 | `int32` | `pkt_type` | 항상 `105` |
| 4 | 4 | `int32` | `pkt_len` | 항상 `20` |
| 8 | 4 | `int32` | `stageState` | 아래 스테이지 상태 |
| 12 | 4 | `int32` | `aliveAllyCount` | 생존 아군 병사 수 |
| 16 | 4 | `int32` | `aliveEnemyCount` | 생존 적군 병사 수 |

| stageState | 이름 | 의미 |
|---:|---|---|
| 0 | `WAITING` | 아직 양 팀 전투가 성립하지 않음 |
| 1 | `RUNNING` | 양 팀에 생존 병사가 있음 |
| 2 | `ALLY_WIN` | 전투 시작 후 아군만 생존 |
| 3 | `ENEMY_WIN` | 전투 시작 후 적군만 생존 |
| 4 | `DRAW` | 전투 시작 후 양 팀 모두 전멸 |

---

## 24. 현재 프로토콜의 알려진 제약

1. 프로토콜 버전은 알리지만 클라이언트와 협상하여 하위 버전으로 내려가지는 않는다.
2. COMMAND_RESULT에 request ID가 없다.
3. snapshot sequence와 server tick 번호가 없다.
4. `unitType`과 영구 `unitID`가 snapshot에 없다.
5. 편입 시 `soldierID`가 바뀌지만 전후 ID 대응표가 없다.
6. 세션 재접속과 게임 복구 기능이 없다.
7. 현재 서버는 인증 키와 권한 토큰을 사용하지 않는다.
8. 변경된 공격력은 snapshot에 포함되지 않아 클라이언트가 직접 조회할 수 없다.
